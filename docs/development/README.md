# 开发记忆

本目录存储从已完成的 Agent 运行中生成的简洁、可审计的开发知识。

- `CURRENT.md`：最新状态，用于快速加载上下文。
- `INDEX.md`：session、ADR 和知识库索引。
- `sessions/`：每次有意义的开发运行一份摘要。
- `decisions/`：已接受的架构决策记录。
- `knowledge/`：稳定可复用的运维知识。

不得存储完整对话、隐藏推理、密钥或完整工具 Trace。
